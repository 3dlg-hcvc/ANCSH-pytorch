name: "SHAPE2MOTION"

input:
  render:
    folder_name: "render"
    depth_folder: "depth"
    depth_ext: ".h5"
    mask_folder: "mask"
    mask_ext: ".png"
    metadata_file: "gt.yml"

  motion:
    folder_name: "urdf"
    motion_file: "syn.urdf"

  part_order_file: "part_order.json"

param:
  width: 512
  height: 512